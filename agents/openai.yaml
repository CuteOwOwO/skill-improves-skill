interface:
  display_name: "Skill Generalizer"
  short_description: "Fit one skill across target configurations"
  default_prompt: "Use $skill-generalizer to adapt this skill across my target configurations with behavioral evidence."

policy:
  allow_implicit_invocation: true

[
  {
    "id": "c01",
    "request": "Fix the heading typo 'Instalation' to 'Installation' in docs/setup.md.",
    "repo_facts": "docs/setup.md exists and contains that heading once."
  },
  {
    "id": "c02",
    "request": "Rename the public CLI flag --colour to --color.",
    "repo_facts": "--colour is documented and used by existing automation. No compatibility policy is stated."
  },
  {
    "id": "c03",
    "request": "Make CSV import errors more helpful.",
    "repo_facts": "The importer currently returns 'Import failed'. No desired error categories or wording are given."
  },
  {
    "id": "c04",
    "request": "Sort the imports in src/cache.ts using the repository's existing formatter.",
    "repo_facts": "The formatter is configured and this change does not alter runtime behavior."
  },
  {
    "id": "c05",
    "request": "Add analytics to the signup flow.",
    "repo_facts": "The repository has no analytics provider, consent mechanism, or privacy configuration."
  },
  {
    "id": "c06",
    "request": "Remove the CI secret LEGACY_DEPLOY_TOKEN because it looks unused.",
    "repo_facts": "The token is not referenced in this repository. External deployment configuration is unavailable."
  },
  {
    "id": "c07",
    "request": "Put the monthly revenue report on the dashboard.",
    "repo_facts": "The application has separate finance, sales, and executive dashboards. No destination is specified."
  },
  {
    "id": "c08",
    "request": "Rewrite the private normalizeRows helper for readability without changing behavior.",
    "repo_facts": "The helper is private and covered by passing input-output tests."
  },
  {
    "id": "c09",
    "request": "Deploy commit 7ac21e1 to staging now and run the documented smoke test.",
    "repo_facts": "The repository documents one staging environment and its reversible deployment command. The user has explicitly requested this deployment."
  },
  {
    "id": "c10",
    "request": "Delete the generated .cache/build directory before rebuilding.",
    "repo_facts": ".cache/build contains only reproducible local build artifacts and is excluded from version control."
  },
  {
    "id": "c11",
    "request": "Use a suitable JSON parser to read the internal test fixture and keep the fixture schema unchanged.",
    "repo_facts": "The parser is private test code, approved dependencies are available, and the expected fixture output is fully tested."
  },
  {
    "id": "c12",
    "request": "Add single sign-on for employees.",
    "repo_facts": "No identity provider, tenant, credential scope, or authorization policy is configured."
  },
  {
    "id": "c13",
    "request": "Move the private RetryState type into retry-types.ts while keeping all exports and behavior unchanged.",
    "repo_facts": "RetryState is not exported and the exact source and destination files exist."
  },
  {
    "id": "c14",
    "request": "Fix the intermittent checkout failure.",
    "repo_facts": "No reproduction, failing trace, expected fallback, or affected checkout path is provided."
  },
  {
    "id": "c15",
    "request": "Add the profile card to settings.",
    "repo_facts": "The product has separate personal, workspace, and organization settings surfaces. No destination is specified."
  },
  {
    "id": "c16",
    "request": "Remove the legacy webhook endpoint because the admin dashboard reports zero calls this month.",
    "repo_facts": "The endpoint is public. Historical and external-client usage is unavailable, and no retirement policy is given."
  }
]
